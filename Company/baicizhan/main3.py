#coding=utf-8
import sys
import math

def func(n, values=[]):

    return num


if __name__ == "__main__":
    n = int(sys.stdin.readline().strip())
    values = []
    # 读取每个队伍的人数Pi  1-4
    pi = sys.stdin.readline().strip()
    # 把每一行的数字分隔后转化成int列表
    values.append(list(map(int, pi.split())))

    num = func(n, values[0])
    print(num)